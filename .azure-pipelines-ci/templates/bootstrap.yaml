steps:
- task: PowerShell@2
  displayName: 'Bootstrap'
  inputs:
    targetType: inline
    pwsh: ${{ parameters.pwsh }}
    script: |
      Import-Module .\tools\appveyor.psm1
      Invoke-AppveyorInstall
